include ./../layout.pug

block content
  h1= title
  form(action="/loans/new", method="post")
    p
      label(for="book") Book
      select(id="book")
        for book in books
          option(value=book.id) #{book.title}
    //- p
    //-   label(for="patron") Patron
    //-   input(type="text", id="patron", name="patron", value=loan.patron_id)
    //- p
    //-   label(for="loaned_on") Loaned On
    //-   input(type="text", id="loaned_on", name="loaned_on", value=loan.loaned_on)
    //- p
    //-   label(for="return_by") Return By:
    //-   input(type="text", id="return_by", name="return_by", value=loan.return_by)